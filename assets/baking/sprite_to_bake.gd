extends Sprite2D


"""
Sprite size needs to match the subviewport dimensions
"""
#func _ready():
	#var subviewport_size = get_parent().size
	#global_position = subviewport_size / 2.0
	#scale = subviewport_size / Vector2i(texture.get_size())
